Sorry, {{ Member.getFirstName }}.  Your booking for {{ Booking.getStartTime.format('G:i a') }} on {{ Booking.getStartTime.format('d F') }} for {{ Booking.getSlots*Club.getBookingIncrement }} minutes could not be made due to a conflict with another booking.
